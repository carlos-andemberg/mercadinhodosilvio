# auth.md

You are an agent. This service supports **agentic registration**: discover → register → (claim if needed) → exchange for an access_token → call API → handle revocation.

The Mercadinho do Silvio website does not require any agent registration or authentication for AI agents. All catalog and location data is fully public and accessible without tokens.